# Ruby 

## Installing Gems / Libraries

`gem install library-name` 

## Printing to the console

`p` includes newline, calls `inspect` instead of `to_s`

`puts` includes newline

`print` does not append new line

## Running Shoes graphical programs

from command line 

`cshoes shoes-file.rb`
