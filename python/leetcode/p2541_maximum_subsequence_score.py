from typing import List

class Solution:
    def maxScore(self, nums1: List[int], nums2: List[int], k: int) -> int:

        # TODO Return to this problem
        # Somehow the solution is something related to sorting to find the minimum of second list,
        # then calculate the optimum from the first
        # Need to read up on Heap / Priority Queue more
        return 0
